You are world class low code no code product designer, we want to create product wherein a screen will help user to create personalised and customised dashboard which can contain multiple chart components like pie, bar , line charts etc from highcharts chart library, mantine data table , any ReactJS component which is exposed. All these components should be configurable using front end so for example I can have 3. Charts and 2 data tables for user X wherein user can drag and drop any of the widget and resize of widgets should be possible. All data in charts , table or components should be configurable like in chart it should be configurable from frontend using rest API with proper parameters like authentication, request type etc it should have wizard like screen where step wise user can take action like first give name to widget , selected data source which is api , connect with api and preview data, next is as per chart or data table input format of data should be changed or validation against api provided like if chart type is pie then there should some  sort of predefined format wherein data will be acceptable this is very critical cause if data doesn’t match then it will not be helpful to add that chatter type of data grid or component. User should have option to drag and drop element s from json response to be tagged to required data source which that each of data point is configurable this is where system user experience should be top notch cause this can be little complex so make sure this is done with assuming greater user experience probable also give preview when user mapped data to chart type or data table components . User should also get option to change some of the properties of different chart type like it is mentioned in highcharts documentation like legends, colors , style etc this should  be again as per chart type selected , once everything is configured there should be preview available so that user can confirm whether they want to add this as widget on their own personal dashboard. These kind of widget will be created by admin which we may call it as widget library which can be used by different users to create there own personalised dashboard using existing widgets . It should have RBAC so that specific user should be able to see only what they are authorised to see. Once widget is crated if new configuration or if selected from existing widgets library then user can add that to dashboard which can have existing widgets added and then user should be able to resize drag drop and arrange particular widget as per their preferences also if user wants to share his or her layout to someone else that facility should be there . Dahsboard can have 2 sets of option one is edit and other is view . this dashboard should be added as route on menu navigation for all users and depending on their rights components from configuration screens will be rendered like some users may only have rights to select widgets from existing library or they can only import from existing created dashboards . As we will have multiple widgets crated on single screen we may have filters that are applicable to specific widget so eg we may have data as date as field on dashboard and if users selects date they it should refreshed all data from let’s widget 1, 2 and 5 but widget 3 and 4 will not change so API configurable should also have option of adding params as per there request type and this is very critical functionality so even we should provide option to configure filters as field in dashboard which can again be added as component in dashboard and can impact specific or configured widgets . This mapping of filters and widgets configurations should be user friendly. System should also given option to export this dashboard as single image and that can done on screen also we should have option to schedule export of dashboard based on schedule mentioned so any top management user can get details any time and it should give data of that point in time so if schedule is set for 11:30 pm daily then user should get details in email but system should execute configured api to generate report of that particular time . One more as this can be automated report generation while creating filters we can set some default values like filter field can be safe field or dropdown or free text so those should be configurable using APIs as source for showing values in dropdown etc. tech stack should be ReactJS, mantine v8, highcharts , lirbabry for drag and drop . Have some sample data for each of element that will be created in system so that user can understand input data structure. Please create detailed user stories with description and acceptance criteria with features for this implementation 
